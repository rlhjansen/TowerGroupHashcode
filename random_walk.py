import classes
import numpy.random as rand
import random
import read_object


def random_walk(images):
    slideshow = []
    while images:
        i = random.randint(0, len(images))
        img = images.pop(i)
        succes = False
        while not success or
